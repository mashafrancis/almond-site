import { Grid } from '@mui/material';

const AdminSupportView = (): JSX.Element => {
	return (
		<Grid container item xs={12} spacing={2}>
			<div>Admin support</div>
		</Grid>
	);
};

export default AdminSupportView;
