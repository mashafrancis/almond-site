import { Grid } from '@mui/material';

const WaterCyclesView = (): JSX.Element => {
	return (
		<Grid container item xs={12} spacing={2}>
			<div>Water</div>
		</Grid>
	);
};

export default WaterCyclesView;
